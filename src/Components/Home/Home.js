import React from 'react';
import Header from '../Header/Header';
import MenuType from '../MenuType/MenuType';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <MenuType></MenuType>
        </div>
    );
};

export default Home;